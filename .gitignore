node_modules/
dist/
build/
coverage/
*.log
.env
.env.*
.DS_Store
!docker/.env.example

# Tooling local do Claude Code / FlowForge — não faz parte do produto e contém
# nomes com ":" (invalido no Windows, quebra o checkout do CI no Windows).
.claude/
